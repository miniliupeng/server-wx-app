import http from '../utils/http'

const OrderAPI={}

/**
 * @description 进行登录操作
 * @param {*} code 临时登录凭证
 * @returns Promise
 */
OrderAPI.list = (params) => {
  return http.post(``)
}
module.exports = OrderAPI
